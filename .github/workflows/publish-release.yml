name: Publish release

on:
  push:
    branches:
      - main
      - release-*

permissions: {}

jobs:
  call:
    uses: holochain/actions/.github/workflows/publish-release.yml@832c0c654691d0ee736a12dab74832a48318fd30 # v1.18.0
    secrets:
      HRA2_GITHUB_TOKEN: ${{ secrets.HRA2_GITHUB_TOKEN }}
      HRA2_CRATES_IO_TOKEN: ${{ secrets.HRA2_CRATES_IO_TOKEN }}
